# Points at the same PrayerHubApp API the mobile app uses. Admin sessions are
# separate from mobile sessions (see docs/02-ARCHITECTURE.md section 6) but hit
# the identical /auth/login endpoint — what makes someone an admin is the
# platform role on their user_roles row, not a different login flow.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
